#!/usr/bin/env bash
###############################################################################
NPM_SCRIPT=$(cd "$(dirname "${BASH_SOURCE[0]}")" >/dev/null 2>&1 && pwd)
###############################################################################

SRC_PATH=$(cd "$NPM_SCRIPT/.." >/dev/null 2>&1 && pwd) ;
TMP_PATH="$(mktemp -dt avalanche-cli-evm-XXXXXXXX)" ;
cp "$SRC_PATH"/cmd/evm/*.sh "$TMP_PATH" -L ;
mv "$TMP_PATH"/*.sh "$SRC_PATH/cmd/evm" ;

###############################################################################
###############################################################################
